// Array.map

class MyNumber {
    MyNumber(num) {
        this.num = num;
    }
}

print [1, 2, 3].map(fun (num) {                                                               // expect [2, 4, 6]
    return num * 2;
});

print [1, 2, 3].map(fun (num) {                                                               // expect [101, 102, 103]
    return num + 100;
});

print [1, 2, 3].map(fun (num) {                                                               // expect [instance of MyNumber, instance of MyNumber, instance of MyNumber]
    return MyNumber(num);
});

print [1, 2, 3].map(fun (num) {                                                               // expect [pad1, pad2, pad3]
    return "pad" + num.toString();
});