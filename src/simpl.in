// Class static-ish methods

class Math {
    exp() {
        for (var idx = 0; idx < 3; idx = idx + 1) {
            print idx;
        }
    }
}

var math = Math(); 

print math.exp();                                                  // expect 8