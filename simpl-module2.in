import "./simpl-module3.in";

print "module 2 evaluated";

class Counter {
    Counter() {
        this.count = 0;
    }

    increment() {
        this.count = this.count + 1;
        return this.count;
    }
}

var counter = Counter();

export counter;