var increment;
var decrement;

fun example() {
    var number = 0;

    fun inc() {
        return (number = number + 1);
    }

    fun dec() {
        return (number = number - 1);
    }

    increment = inc;
    decrement = dec;
}

example();

print increment();
print increment();
print increment();
print increment();
print decrement();
print decrement();
print decrement();