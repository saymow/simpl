fun example() {
    var number = 0;

    fun inc() {
        return (number = number + 1);
    }

    return inc;
}

var closure = example();

print closure();
print closure();
print closure();
print closure();
